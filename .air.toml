root = "."
tmp_dir = "tmp"

[build]
cmd = "go build -o tmp\\app.exe cmd\\server\\main.go"
bin = "tmp\\app.exe"
full_bin = "tmp\\app.exe"

include_ext = ["go"]
exclude_dir = ["tmp", "vendor", ".git"]

delay = 1000
stop_on_error = true
send_interrupt = true
kill_delay = "8s"

[log]
time = true
